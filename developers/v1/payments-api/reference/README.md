---
icon: book-open
description: Operation reference for the v1 Payments API.
---

# Reference

The pages below are auto-generated from the `evolve-payments-v1` OpenAPI spec. Every operation has its own page with the full parameter list, request and response schemas, code samples in Node, Python, Go, Ruby, and cURL, and an interactive **Test it** panel.

{% hint style="warning" icon="triangle-exclamation" %}
**v1 is deprecated.** New integrations should use v2. v1 endpoints will return `410 Gone` after 2026-12-31. Switch to the v2 variant in the top bar for the current API.
{% endhint %}

## Spec source

```
../../openapi/v1/payments.yaml
```

The spec is uploaded to GitBook as `evolve-payments-v1` and synced from this file on every release.

## Related

* [Overview](../README.md) — deprecation notice and migration guide.
* [Conventions](../../getting-started/conventions.md) — what's true across every operation.
* [Webhooks → Event catalog](../../webhooks/event-catalog.md) — the events Payments emits.
